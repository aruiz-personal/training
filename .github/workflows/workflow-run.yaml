name: Workflow Run
on:
    workflow_run:
        workflows: [name: Repository Events]
        types: [completed]
jobs:
    echo-string:
        runs-on: ubuntu-latest
        steps:
            - run: echo "I was triggered cos 'Repository Events' was completed"
